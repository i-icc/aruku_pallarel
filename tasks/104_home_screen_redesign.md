# ホーム画面の刷新 (Task 104)

## 背景
- UI を大幅に変更し、地図をメイン背景とした没入感のあるデザインにする
- 既存のボトムナビゲーションや情報表示を廃止し、散歩中画面 (`WalkScreen`) に近い構成にする
- `spec/images/home.PNG` のデザインに合わせる

## ゴール
- ホーム画面 (`HomeScreen`) を地図背景＋オーバーレイ UI に変更する
- ボトムナビゲーションバーを廃止する
- 以下のボタンを画面下部に配置する
  - 中央: 「散歩する」ボタン（`AppGradientPillButton` を使用、散歩終了ボタンと同じ見た目）
  - 左: 「履歴」ボタン（`WalkScreen` のチャットボタン等の丸いフローティングボタンスタイル）
  - 右: 「設定」ボタン（同上）
- 既存の「今日の記録」などの情報表示は（一旦）非表示にする

## ToDo
- [x] ウィジェットの共通化
  - [x] `WalkScreen` にある `_ChatFloatingButton` を汎用的な `AppFloatingButton` (仮) にリファクタリングする
- [x] `HomeScreen` の実装変更
  - [x] `FlutterMap` を背景に配置する (現在位置を表示)
  - [x] ボトムナビゲーションバー (`Scaffold.bottomNavigationBar`) を削除する
  - [x] 画面下部に `Stack` でボタンを配置する
    - [x] 左: 履歴画面へ遷移するボタン
    - [x] 右: 設定画面へ遷移するボタン
    - [x] 中央: 「散歩する」ボタン
- [x] 遷移ロジックの調整
  - [x] 履歴ボタンタップ -> `HistoryRoute`
  - [x] 設定ボタンタップ -> `SettingsRoute`
  - [x] 散歩するボタンタップ -> 既存の散歩開始フロー
- [x] 不要になったウィジェットやコードの整理（コメントアウトまたは削除）

## 受け入れ条件
- アプリ起動直後のホーム画面が地図背景になっている
- ボトムバーが表示されていない
- 下部に「履歴」「散歩する」「設定」の3つのボタンが表示されている
- 各ボタンが正しく機能し、それぞれの画面に遷移できる
- 「散歩する」ボタンのデザインが散歩終了時等のグラデーションボタンと一致している
- 履歴・設定ボタンが丸いフローティングスタイルになっている
